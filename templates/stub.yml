---
meta:
  environment: ENVIRONMENT
  dns_root: microbosh
  security_groups:
    - pgbackup
  stemcell:
    name: STEMCELL

director_uuid: DIRECTOR-UUID

releases:
  - name: pgbackup
    version: latest
